# Optional: for `wrangler pages dev` / local function testing.
# Pages project settings in the dashboard override this for production.

name = "andrewriley-www"
pages_build_output_dir = "out"
compatibility_date = "2025-04-01"
